import logging
from telegram import Update, ReplyKeyboardMarkup, KeyboardButton
from telegram.ext import Application, CommandHandler, MessageHandler, filters, ContextTypes, ConversationHandler
import requests
from bs4 import BeautifulSoup

# Включим логирование, чтобы видеть ошибки
logging.basicConfig(
    format='%(asctime)s - %(name)s - %(levelname)s - %(message)s', level=logging.INFO
)
logger = logging.getLogger(__name__)

# Токен бота (ЗАМЕНИТЕ НА СВОЙ!)
BOT_TOKEN = "YOUR_BOT_TOKEN_HERE"

# Ключи для состояния разговора (ConversationHandler)
AUTOSTART, CONTROL, GPS, PHONE = range(4)

# Данные для рекомендации (глобальная переменная для простоты, в продакшене используйте БД)
user_data = {}

# Функция для отправки данных в CRM
def send_to_crm(phone_number):
    """Отправляет номер телефона в CRM через веб-форму"""
    url = "https://ya7auto.ru/crm/form/iframe/4/"
    form_data = {
        'phone': phone_number
    }
    try:
        response = requests.post(url, data=form_data)
        if response.status_code == 200:
            logging.info(f"Номер {phone_number} успешно отправлен в CRM.")
            return True
        else:
            logging.error(f"Ошибка при отправке в CRM. Код: {response.status_code}")
            return False
    except Exception as e:
        logging.error(f"Исключение при отправке в CRM: {e}")
        return False

# Функция для парсинга данных о продуктах (кэшируется при запуске)
def get_products_from_table():
    """Парсит данные о продуктах с сайта и возвращает список словарей"""
    url = "https://ya7auto.ru/auto-security/car-alarms/"
    try:
        response = requests.get(url)
        soup = BeautifulSoup(response.text, 'html.parser')

        products = []
        # Это упрощенный пример. Селекторы нужно подобрать под реальную HTML-структуру вашего сайта!
        # Ищем таблицу или список продуктов
        product_cards = soup.find_all('div', class_='product-card')  # Примерный селектор

        for card in product_cards:
            try:
                name_elem = card.find('h3')
                link_elem = card.find('a')
                # ... парсим остальные данные (автозапуск, брелок, GSM, GPS) из карточки
                # Это сложная и ненадежная часть без точных классов.

                product = {
                    'name': name_elem.text.strip() if name_elem else "Название не найдено",
                    'link': link_elem['href'] if link_elem else "#",
                    'autostart': 1, # Заглушки
                    'remote': 1,
                    'gsm': 0,
                    'gps': 0
                }
                products.append(product)
            except Exception as e:
                logger.error(f"Ошибка парсинга карточки товара: {e}")
                continue

        # Если не получилось распарсить, используем жестко заданный список как запасной вариант
        if not products:
            products = [
                {'name': 'Pandora DX-40R', 'autostart': 0, 'remote': 1, 'gsm': 0, 'gps': 0, 'link': 'https://ya7auto.ru/auto-security/car-alarms/pandora-dx-40r/'},
                {'name': 'Pandora DX-40RS', 'autostart': 1, 'remote': 1, 'gsm': 0, 'gps': 0, 'link': 'https://ya7auto.ru/auto-security/car-alarms/pandora-dx-40rs/'},
                # ... Вставьте ВЕСЬ остальной список из задания здесь
                {'name': 'StarLine S96 V2 LTE GPS', 'autostart': 1, 'remote': 0, 'gsm': 1, 'gps': 1, 'link': 'https://ya7auto.ru/auto-security/car-alarms/starline-s96-v2-lte-gps/'}
            ]
        return products

    except Exception as e:
        logger.error(f"Ошибка при получении данных с сайта: {e}. Используется резервный список.")
        # Возвращаем жестко заданный список в случае ошибки
        return [
            {'name': 'Pandora DX-40R', 'autostart': 0, 'remote': 1, 'gsm': 0, 'gps': 0, 'link': 'https://ya7auto.ru/auto-security/car-alarms/pandora-dx-40r/'},
            # ... Вставьте ВЕСЬ остальной список из задания здесь
            {'name': 'StarLine S96 V2 LTE GPS', 'autostart': 1, 'remote': 0, 'gsm': 1, 'gps': 1, 'link': 'https://ya7auto.ru/auto-security/car-alarms/starline-s96-v2-lte-gps/'}
        ]

# Загружаем данные о продуктах при старте
PRODUCTS_DATA = get_products_from_table()

async def start(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Начинает опрос, задает первый вопрос."""
    user = update.message.from_user
    await update.message.reply_text(
        f"Сообщение 1.1 👋🏻 Приветствуем, {user.first_name}!\n\n"
        "Готов помочь подобрать идеальную систему для твоего автомобиля!\n\n"
        "Сообщение 1.2 🦾 Давай определимся с ключевыми функциями\n\n"
        "Сообщение 1.3 ☀️ Подавляющее большинство наших клиентов выбирают систему с главной целью — реализовать дистанционный запуск двигателя.\n\n"
        "Сообщение 1.4 В нашем климате прогрев двигателя перед поездкой — это необходимость. Даже при небольшом минусе это значительно снижает износ мотора.\n\n"
        "Сообщение 1.5 Ну и конечно, садиться в уже тёплый и комфортный салон — это просто приятно.\n\n"
        "Сообщение 1.6 Какая функция для вас в приоритете?",
        reply_markup=ReplyKeyboardMarkup(
            [["С автозапуском", "БЕЗ автозапуска"]],
            one_time_keyboard=True,
            resize_keyboard=True
        ),
    )
    return AUTOSTART

async def autostart_choice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Обрабатывает выбор автозапуска и задает второй вопрос."""
    choice = update.message.text
    user_data[update.message.from_user.id] = {'autostart': 1 if choice == 'С автозапуском' else 0}

    await update.message.reply_text(
        "Сообщение 2.1 📡 Теперь давай выберем способ управления\n\n"
        "Сообщение 2.2 🙄 Есть устаревший метод — управление с брелока сигнализации. Его минус в нестабильном сигнале: есть риск не получить оповещение о тревоге. Поэтому мы рекомендуем более современный вариант — управление со смартфона.\n\n"
        "Сообщение 2.3 ☺️ Через мобильное приложение ты сможешь дистанционно открывать и закрывать авто, отслеживать его местоположение и статус, настраивать датчики и многое другое. Главное — ты гарантированно получишь пуш-уведомление о любом происшествии, где бы ты ни был.\n\n"
        "Сообщение 2.4 Как вам удобнее управлять системой?",
        reply_markup=ReplyKeyboardMarkup(
            [["😎 Приложение в телефоне", "📺 Брелок"]],
            one_time_keyboard=True,
            resize_keyboard=True
        ),
    )
    return CONTROL

async def control_choice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Обрабатывает выбор управления и задает третий вопрос."""
    choice = update.message.text
    user_data[update.message.from_user.id]['control'] = 'app' if 'Приложение' in choice else 'remote'

    await update.message.reply_text(
        "Сообщение 3.1 🔥 Отлично! Мы почти подобрали твою идеальную систему. Остался последний шаг.\n\n"
        "Сообщение 3.2 Если ты часто передаешь ключи другим людям или тебе критично важно отслеживать каждое перемещение автомобиля, то тебе нужна система со встроенным GPS-модулем.\n\n"
        "Сообщение 3.3 Он позволит тебе в реальном времени видеть точное местоположение машины, а в приложении можно будет посмотреть детальный маршрут ее поездки.\n\n"
        "Нужен ли вам GPS-модуль для отслеживания?",
        reply_markup=ReplyKeyboardMarkup(
            [["Да, нужен GPS", "Нет, не нужен"]],
            one_time_keyboard=True,
            resize_keyboard=True
        ),
    )
    return GPS

async def gps_choice(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Обрабатывает выбор GPS, показывает рекомендацию и запрашивает телефон."""
    choice = update.message.text
    user_data[update.message.from_user.id]['gps'] = 1 if 'Да' in choice else 0

    user_prefs = user_data[update.message.from_user.id]
    recommended_products = []

    # Логика подбора: ищем системы, подходящие под выбор пользователя
    for product in PRODUCTS_DATA:
        # Проверяем автозапуск
        if user_prefs['autostart'] == 1 and product['autostart'] == 0:
            continue
        # Проверяем управление: если пользователь выбрал приложение, нужен GSM модуль (gsm=1)
        # Если выбрал брелок, то подойдет и система без GSM (gsm=0 или 1)
        if user_prefs['control'] == 'app' and product['gsm'] == 0:
            continue
        # Проверяем GPS
        if user_prefs['gps'] == 1 and product['gps'] == 0:
            continue

        recommended_products.append(product)
        # Ограничимся двумя вариантами
        if len(recommended_products) == 2:
            break

    # Формируем сообщение с рекомендациями
    if recommended_products:
        message_text = "Вот отличные варианты для вас:\n\n"
        for prod in recommended_products:
            message_text += f"• <a href='{prod['link']}'>{prod['name']}</a>\n"
        message_text += "\nДля подробного обсуждения и оформления заказа оставьте, пожалуйста, ваш номер телефона. Наш специалист свяжется с вами в ближайшее время."
    else:
        message_text = "К сожалению, по вашим запросам не найдено подходящих систем. Оставьте ваш номер телефона, и наш специалист поможет вам с подбором вручную."

    await update.message.reply_text(message_text, parse_mode='HTML')

    await update.message.reply_text(
        "Пожалуйста, отправьте ваш номер телефона. Используйте кнопку ниже для удобства.",
        reply_markup=ReplyKeyboardMarkup(
            [[KeyboardButton("📞 Отправить мой номер", request_contact=True)]],
            one_time_keyboard=True,
            resize_keyboard=True
        ),
    )
    return PHONE

async def get_phone(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Обрабатывает полученный контакт и отправляет его в CRM."""
    phone_number = None

    if update.message.contact:
        phone_number = update.message.contact.phone_number
    else:
        # Если пользователь ввел номер вручную
        phone_number = update.message.text

    if phone_number:
        # Отправляем номер в CRM
        success = send_to_crm(phone_number)
        if success:
            await update.message.reply_text(
                "Спасибо! Ваш номер принят. Мы скоро с вами свяжемся!",
                reply_markup=ReplyKeyboardMarkup([[]], resize_keyboard=True)  # Убираем клавиатуру
            )
        else:
            await update.message.reply_text(
                "Произошла ошибка. Попробуйте отправить номер позже или свяжитесь с нами другим способом."
            )
    else:
        await update.message.reply_text("Не удалось распознать номер. Попробуйте еще раз.")

    # Завершаем диалог
    return ConversationHandler.END

async def cancel(update: Update, context: ContextTypes.DEFAULT_TYPE) -> int:
    """Отменяет опрос."""
    await update.message.reply_text('Диалог прерван. Если нужна помощь, начните заново с /start.')
    return ConversationHandler.END

def main() -> None:
    """Запускает бота."""
    # Создаем Application и передаем ему токен бота
    application = Application.builder().token(BOT_TOKEN).build()

    # Настраиваем обработчик диалога (ConversationHandler)
    conv_handler = ConversationHandler(
        entry_points=[CommandHandler('start', start)],
        states={
            AUTOSTART: [MessageHandler(filters.Regex("^(С автозапуском|БЕЗ автозапуском)$"), autostart_choice)],
            CONTROL: [MessageHandler(filters.Regex("^(😎 Приложение в телефоне|📺 Брелок)$"), control_choice)],
            GPS: [MessageHandler(filters.Regex("^(Да, нужен GPS|Нет, не нужен)$"), gps_choice)],
            PHONE: [
                MessageHandler(filters.CONTACT, get_phone),
                MessageHandler(filters.TEXT & ~filters.COMMAND, get_phone)
            ],
        },
        fallbacks=[CommandHandler('cancel', cancel)],
    )

    application.add_handler(conv_handler)

    # Запускаем бота
    application.run_polling()

if __name__ == '__main__':
    main()